name = "flight"
main = "worker.js"
compatibility_date = "2026-07-18"

[vars]
DEFAULT_MARKET = "hk"
# TP_MARKER = "123456"

# Secrets — set in the dashboard, NOT here:
#   TRAVELPAYOUTS_TOKEN   (required)
#   GEMINI_API_KEY        (optional — AI analysis; falls back to rules)
