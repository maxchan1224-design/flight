name = "flight"
main = "worker.js"
compatibility_date = "2026-07-18"

[vars]
DEFAULT_MARKET = "hk"
# TP_MARKER = "123456"   # your Travelpayouts marker, appended to booking links

# Secret — set in the dashboard as an encrypted Secret, NOT here:
#   TRAVELPAYOUTS_TOKEN
