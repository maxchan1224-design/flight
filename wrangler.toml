name = "flight"
main = "worker.js"
compatibility_date = "2026-07-19"

# Optional. Stores your preferences, ground-transport costs, and price history.
# The app works fully without it — you just lose the "price context" panel.
# [[kv_namespaces]]
# binding = "HISTORY"
# id = "PASTE_REAL_KV_NAMESPACE_ID"

[vars]
DEFAULT_MARKET = "hk"
# TP_MARKER = "123456"

# Secrets — dashboard only:
#   TRAVELPAYOUTS_TOKEN   (required)
#   GEMINI_API_KEY        (optional)
